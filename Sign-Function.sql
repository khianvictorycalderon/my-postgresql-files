-- Returns either -1, 0, or 1, depending on the sign of X
SELECT SIGN(4.46); -- Returns 1 because 4.46 is a positive number
SELECT SIGN(0); -- Returns 0 well because it is 0
SELECT SIGN(-3.23); -- Returns 1 because -3.23 is a negative number