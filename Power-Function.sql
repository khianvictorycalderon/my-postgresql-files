-- Returns the X to the power of Y, basically X multipying Y times to itself
SELECT POW(5, 3); -- Basically means 5 times 5 times 5

-- Bonus: For getting the cube root
SELECT POWER(125, 1.0/3.0); -- Basically kind of reverse engineering (Must have decimal)